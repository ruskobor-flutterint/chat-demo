import { Transform, TransformCallback } from "stream";

export default class Broadcaster extends Transform {
  //   outputs: WebSocket[]; // Map

  // Chat broadcaster =>
  /**
   *
   *
   */

  // Chat

  constructor(options = {}) {
    super(options);
  }

  _transform(
    chunk: any,
    encoding: BufferEncoding,
    callback: TransformCallback
  ): void {}

  //   subscribe(user): void {

  //   }
}
